import { Schema } from "mongoose";
/**
 *
 */
export declare enum InitCounter {
    YEARLY = "yearly",
    MONTHLY = "monthly",
    DAILY = "daily",
    HOURLY = "hourly"
}
/**
 *
 */
export declare type Options = {
    field: string;
    prefix: string;
    format: string;
    separator: string;
    initCounter: "yearly" | "monthly" | "daily" | "hourly";
    digits: number;
};
/**
 *
 * @param counter
 * @param size
 */
export declare const addZeros: (counter: number, size: number) => string;
/**
 *
 * @param options
 * @param serial
 */
export declare const extractCounter: (options: Options, serial: string) => string;
/**
 *
 * @param schema
 * @param options
 */
export declare const plugin: (schema: Schema, options: Options) => void;
