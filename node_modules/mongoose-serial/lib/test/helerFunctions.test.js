"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var _a = require('../'), addZeros = _a.addZeros, extractCounter = _a.extractCounter, InitCounter = _a.InitCounter;
var options = { field: 'serial', prefix: "Invoice", format: "PREFIX", separator: "-", digits: 5, initCounter: "daily" };
describe('Helper-functions', function () {
    it('should add lead zeros to integer number', function () {
        var count = 3;
        var size = 5;
        var ret = addZeros(count, size);
        chai_1.expect(ret).to.equal('00003');
        count = 300;
        size = 6;
        ret = addZeros(count, size);
        chai_1.expect(ret).to.equal('000300');
    });
    it('should extract and increment counter number daily', function () {
        var serial = ["INVOICE", "2021", "03", "07", "00301"].join(options.separator);
        var ret = extractCounter(__assign(__assign({}, options), { initCounter: InitCounter.DAILY }), serial);
        chai_1.expect(ret).to.equal('00001');
    });
    it('should extract and increment counter number monthly', function () {
        var serial = ["INVOICE", "2021", "03", "00301"].join(options.separator);
        var ret = extractCounter(__assign(__assign({}, options), { initCounter: "monthly" }), serial);
        chai_1.expect(ret).to.equal('00302');
    });
    it('should extract and increment counter number yearly', function () {
        var serial = ["INVOICE", "2021", "00301"].join(options.separator);
        var ret = extractCounter(__assign(__assign({}, options), { initCounter: "yearly" }), serial);
        chai_1.expect(ret).to.equal('00302');
    });
    it('should extract and increment counter number with 1o digits', function () {
        var serial = ["INVOICE", "2021", "00301"].join(options.separator);
        var ret = extractCounter(__assign(__assign({}, options), { initCounter: undefined, digits: undefined }), serial);
        chai_1.expect(ret).to.equal('0000000302');
    });
});
